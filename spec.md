# Specification

## Summary
**Goal:** Remove the black empty space that appears below the footer in desktop view.

**Planned changes:**
- Fix CSS/layout so that no extra black or dark region appears below the footer in desktop view
- Ensure the page ends cleanly at the bottom edge of the footer with no overflow, extra height, or background causing the black area

**User-visible outcome:** In desktop view, the page ends cleanly at the footer with no black space beneath it. Mobile layout and footer content remain unchanged.
